{
  "name": "fastify-prisma-app",
  "version": "1.0.0",
  "main": "src/server.ts",
  "scripts": {
    "prisma:migrate": "prisma migrate dev --name init",
    "prisma:generate": "prisma generate",
    "serve":"nodemon src/server.ts",
    "dev": "ts-node-dev --respawn src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "fastify": "^4.24.0",
    "prisma": "^5.0.0",
    "@prisma/client": "^5.0.0",
    "pg": "^8.11.0",
    "typeorm": "^0.3.20",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
